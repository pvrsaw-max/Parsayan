import React from"react";import GameShell from"../components/GameShell";
export default function Winner({s,newGame}){const a=[...s.players].sort((x,y)=>s.scores[y]-s.scores[x]);return <GameShell><div className="winner"><div className="winnerHalo">♛</div><span className="eyebrow">GAME OVER</span><p>برنده پارسایان</p><h1>{s.winner||a[0]}</h1><strong className="winnerScore">{s.scores[s.winner||a[0]]}</strong><small>امتیاز</small></div><div className="ranking">{a.map((n,i)=><div className={i===0?"rank first":"rank"} key={n}><span>{String(i+1).padStart(2,"0")}</span><b>{n}</b><strong>{s.scores[n]}</strong></div>)}</div><button className="cta" onClick={newGame}>بازی جدید ←</button></GameShell>}